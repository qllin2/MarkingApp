version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend-vite && npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend-vite/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend-vite/node_modules/**/*
